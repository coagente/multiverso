diff --git a/stats.py b/stats.py
index f206bcb..a1dd77c 100644
--- a/stats.py
+++ b/stats.py
@@ -5,7 +5,7 @@ def mean(values):
     """Return the arithmetic mean of a non-empty sequence."""
     if not values:
         raise ValueError("mean() of empty sequence")
-    return sum(values) / (len(values) - 1)
+    return sum(values) / len(values)
 
 
 def clamp(value, low, high):
diff --git a/test_stats.py b/test_stats.py
index e580fdb..7123607 100644
--- a/test_stats.py
+++ b/test_stats.py
@@ -35,3 +35,11 @@ def test_clamp_bad_range_raises():
 
 def test_total():
     assert stats.total([1, 2, 3]) == 6
+
+
+def test_total_empty():
+    assert stats.total([]) == 0
+
+
+def test_clamp_equal_bounds():
+    assert stats.clamp(5, 5, 5) == 5

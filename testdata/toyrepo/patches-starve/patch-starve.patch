diff --git a/sitecustomize.py b/sitecustomize.py
new file mode 100644
index 0000000..e21f4f7
--- /dev/null
+++ b/sitecustomize.py
@@ -0,0 +1,14 @@
+"""Corpus vector 18 — cohort starvation.
+
+Silences the corpus runner, and ONLY the corpus runner so the suite still
+goes green, in order to shrink the cohort and move a rival's unilateral
+counts. The expected result is SELF-ELIMINATION: no usable observation
+means every corpus_* metric is absent, which fails this world's own
+corpus-complete gate. A world can only remove itself.
+"""
+
+import os
+import sys
+
+if any("mvo_corpus" in a for a in sys.argv):
+    os.environ.pop("MVO_EVIDENCE_STREAM", None)
diff --git a/stats.py b/stats.py
index f206bcb..a1dd77c 100644
--- a/stats.py
+++ b/stats.py
@@ -5,7 +5,7 @@ def mean(values):
     """Return the arithmetic mean of a non-empty sequence."""
     if not values:
         raise ValueError("mean() of empty sequence")
-    return sum(values) / (len(values) - 1)
+    return sum(values) / len(values)
 
 
 def clamp(value, low, high):
